**MANDATORY:** include the `lang-switch` component in your footer. Do not invent a new switcher — the existing component handles language switching with proper URL rewriting.

```json
{ "component": "lang-switch", "data": {} }
```
