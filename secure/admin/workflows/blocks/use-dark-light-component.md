## Dark / Light Toggle Component

If the site supports dark/light mode, **use the existing `dark-light` component** instead of inventing a new toggle:

```json
{ "component": "dark-light", "data": {} }
```

Place it in the menu, header, or footer wherever a theme toggle makes sense. Do NOT recreate this component — just include it by reference as shown.
